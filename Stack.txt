The Map Engine: MapLibre GL JS
The Map Data: OpenStreetMap (OSM)

This script loading failure usually happens because of strict network firewalls, ad-blockers, or a typo in the link. Let's fix this right now by downloading MapLibre directly into your project files so it loads locally without relying on external CDNs.

Feature: geospatial filtering
Steps:
- A map, tile layer, and markers
- Data as an array of objects
- A distance calculation (haversine)
- Conditional filtering based on that calculation
